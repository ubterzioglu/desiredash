# Desiremap UI & Database Updates

## Overview
Update the application to replace "Corteqs" branding with "Desiremap" throughout, restructure tabs, add radio buttons for classification, and implement database-driven functionality for draft notes, follow-up tracking, and document management.

## Changes Summary

### 1. Branding Changes
- [ ] Replace all "Corteqs" text with "Desiremap" across the application
- [ ] Replace "MVP V2.0" header with "Desiremaple zevkten dört köşe!" in top-right

### 2. Tab Structure Reorganization
Current structure (if different) should be reorganized to:
- [ ] **Tab 1: Why? (Neden ?)** - Display with "coming soon" message
- [ ] **Tab 2: Draft Note (Draft Not)** - Active, with radio button functionality
- [ ] **Tab 3: Follow-up Table (Takip Tablosu)** - Active, with assignment functionality
- [ ] Other tabs - Keep but display "coming soon" message when clicked

### 3. Draft Notes Section (Tab 2)
- [ ] Add radio buttons for classification: BK, SY, UBT
- [ ] Connect radio buttons to database updates
- [ ] Save selected classification to database

### 4. Follow-up Table Section (Tab 3)
- [ ] Add "Assigned" (Atanan) field with options:
  - Sahin
  - Baran
  - UBT
- [ ] Connect assignment dropdown to database updates
- [ ] Save assignments to database

### 5. Documents Section
- [ ] Clear/reset documents section UI
- [ ] Add document/link add function
- [ ] Form fields: Document/Link Title and Link URL
- [ ] Functionality: Save → direct transition to list display

### 6. Database Schema Updates
- [ ] Update draft_notes table to include classification field (BK, SY, UBT)
- [ ] Update follow_up/takip table to include assigned_to field (enum: Sahin, Baran, UBT)
- [ ] Verify documents/links table schema supports title and URL

## Implementation Steps
1. Read current codebase structure to understand pages and components
2. Identify all "Corteqs" references
3. Update components with new tab structure
4. Add radio buttons and dropdown components
5. Create/update database queries for new fields
6. Implement document add/save functionality
7. Update header branding

## Database Tables to Modify / Create
- `draft_notlar` - Add `classification` field (BK, SY, UBT) if not exists
- `gorevler` - Add `assigned_to` field (Sahin, Baran, UBT) if not exists
- `dosya_linkler` (NEW) - Create new table for document/link storage
  - Fields: id, title, url, created_at, (other metadata as needed)

## UI/UX Details
- "Coming soon" message displays in **modal** (not inline)
- No URL validation required on document add
- Documents should directly transition to list after save

---

**Status**: Ready for implementation
